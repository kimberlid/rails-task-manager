<h1>My Tasks</h1>
<ul style="list-style: none;">
  <% @tasks.each do |task| %>
    <li>
      <input type="checkbox" disabled <%= task.completed ? 'checked' : ''%>>
      <%= link_to(task.title, task_path(task.id))%>
    </li>
  <% end %>
</ul>
<p><%= link_to 'New Tasks', tasks_new_path %></p>
